COMPOSE_PROJECT_NAME=fantasy-hockey
DEV_TAG=dev

# Local-dev-only value, not a real secret. A real deployment overrides this
# with a long, random value - see README.md's Configuration section.
SESSION_SECRET=local-dev-only-insecure-secret
