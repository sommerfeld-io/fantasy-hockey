# Package: `clock`

Provides the current date and time for the application.

`Now` returns the current date and time formatted as RFC 3339. This is a placeholder implementation used to keep the build, unit test, and acceptance test pipelines in place while the real application logic is developed in later steps.
