# fantasy-hockey

Entry point for the fantasy-hockey binary. `main` is currently a placeholder: it prints the current date and time via `internal/clock`. All application logic lives in the `internal/` packages; `main` only wires them together.
